"""
constants.py

Все "магические" значения проекта (пути, названия, цвета, размеры) собраны
здесь в одном месте, а не разбросаны по коду. Это отдельно требуется
критериями оценивания (обозначение необходимых констант).
"""

import os

# ---------------------------------------------------------------------------
# Пути к файлам проекта.
# BASE_DIR вычисляется от расположения этого файла, а не от текущей рабочей
# директории — иначе после сборки в .exe пути могут "поехать" в зависимости
# от того, откуда пользователь запустил программу.
# ---------------------------------------------------------------------------
BASE_DIR = os.path.dirname(os.path.abspath(__file__))

DB_PATH = os.path.join(BASE_DIR, "planora.db")

UI_DIR = os.path.join(BASE_DIR, "ui")
MAIN_WINDOW_UI = os.path.join(UI_DIR, "main_window.ui")
TASK_DIALOG_UI = os.path.join(UI_DIR, "task_dialog.ui")
CATEGORIES_DIALOG_UI = os.path.join(UI_DIR, "categories_dialog.ui")

ICONS_DIR = os.path.join(BASE_DIR, "assets", "icons")
STYLE_PATH = os.path.join(BASE_DIR, "styles", "style.qss")

# ---------------------------------------------------------------------------
# Название приложения.
# ---------------------------------------------------------------------------
APP_NAME = "Planora"

# ---------------------------------------------------------------------------
# Приоритеты задач.
# ---------------------------------------------------------------------------
PRIORITY_HIGH = "Высокий"
PRIORITY_MEDIUM = "Средний"
PRIORITY_LOW = "Низкий"

# Порядок важен: используется для сортировки задач по приоритету.
PRIORITIES = [PRIORITY_HIGH, PRIORITY_MEDIUM, PRIORITY_LOW]

PRIORITY_COLORS = {
    PRIORITY_HIGH: "#ef4444",    # красный
    PRIORITY_MEDIUM: "#f59e0b",  # оранжевый
    PRIORITY_LOW: "#71717a",     # серый (zinc-500)
}

# ---------------------------------------------------------------------------
# Категории, создаваемые автоматически при первом запуске приложения.
# Формат: (название, цвет в HEX).
# ---------------------------------------------------------------------------
DEFAULT_CATEGORIES = [
    ("Учёба", "#6366f1"),
    ("Работа", "#0ea5e9"),
    ("Личное", "#22c55e"),
    ("Дом", "#f59e0b"),
    ("Здоровье", "#ec4899"),
]

# ---------------------------------------------------------------------------
# Состояния дедлайна (текст метки на карточке задачи) и их цвета.
# ---------------------------------------------------------------------------
DEADLINE_OVERDUE = "Просрочено"
DEADLINE_TODAY = "Сегодня"
DEADLINE_TOMORROW = "Завтра"
DEADLINE_DONE = "Выполнено"
DEADLINE_NO_DATE = "Без срока"
# Для остальных случаев метка формируется как "Через {N} дн.".
DEADLINE_SOON_TEMPLATE = "Через {} дн."

# Текст такой метки каждый раз разный ("Через 3 дн.", "Через 10 дн."),
# поэтому цвет для него хранится под отдельным служебным ключом.
DEADLINE_SOON_KEY = "soon"

DEADLINE_STATE_COLORS = {
    DEADLINE_OVERDUE: "#ef4444",
    DEADLINE_TODAY: "#f59e0b",
    DEADLINE_TOMORROW: "#eab308",
    DEADLINE_SOON_KEY: "#71717a",
    DEADLINE_DONE: "#22c55e",
    DEADLINE_NO_DATE: "#a1a1aa",
}

# ---------------------------------------------------------------------------
# Форматы дат: в БД дата хранится в ISO-формате (сортируется как строка),
# пользователю показывается в привычном виде дд.мм.гггг.
# ---------------------------------------------------------------------------
DATE_FORMAT_DB = "yyyy-MM-dd"
DATE_FORMAT_DISPLAY = "dd.MM.yyyy"

# ---------------------------------------------------------------------------
# Размеры главного окна по умолчанию.
# ---------------------------------------------------------------------------
MAIN_WINDOW_MIN_WIDTH = 960
MAIN_WINDOW_MIN_HEIGHT = 600

# ---------------------------------------------------------------------------
# Индексы страниц QStackedWidget (режимы главного окна).
# Порядок страниц задан в main_window.ui: сначала calendarPage, потом
# allTasksPage. Держим номера здесь, чтобы в коде не встречались
# безымянные 0 и 1.
# ---------------------------------------------------------------------------
MODE_CALENDAR_INDEX = 0
MODE_ALL_TASKS_INDEX = 1

# ---------------------------------------------------------------------------
# Подписи, которые код подставляет в виджеты во время работы.
# ---------------------------------------------------------------------------
# Первый пункт фильтра по категории — "показывать все".
CATEGORY_FILTER_ALL = "Все категории"

# Подпись над списком задач выбранного дня в режиме "Календарь".
SELECTED_DAY_LABEL_TEMPLATE = "Задачи на {}"

# Что показать, когда список задач пуст.
EMPTY_DAY_TASKS_TEXT = "На этот день задач нет"
EMPTY_ALL_TASKS_TEXT = "Задач пока нет"

# ---------------------------------------------------------------------------
# Карточка задачи в списке.
# ---------------------------------------------------------------------------
# Ширина цветной полоски категории слева и её скругление.
CARD_COLOR_BAR_WIDTH = 4
CARD_COLOR_BAR_RADIUS = 2

# Минимальная высота карточки и внутренние отступы.
CARD_MIN_HEIGHT = 64
CARD_MARGIN = 10
CARD_SPACING = 10

# Высота полоски прогресса подзадач.
CARD_PROGRESS_HEIGHT = 14

# Цвет полоски и подпись для задачи без категории.
CATEGORY_COLOR_DEFAULT = "#d4d4d8"
NO_CATEGORY_TEXT = "Без категории"

# Подпись на полоске прогресса и всплывающая подсказка карточки.
SUBTASKS_PROGRESS_TEMPLATE = "{} из {}"
CARD_TOOLTIP_TEMPLATE = "Категория: {}"
DONE_CHECKBOX_TOOLTIP = "Отметить выполненной"
